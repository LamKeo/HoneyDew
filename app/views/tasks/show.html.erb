<!-- corresponding view to the show action in tasks_controller.rb -->

<h2><%= @task.title %></h2>
<p><%= @task.description %></p>

<%= link_to "Back", root_path %>
<%= link_to "Edit", edit_task_path(@task) %>
